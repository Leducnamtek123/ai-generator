# Test Matrix

This file maps product behavior to proof.

No product behavior has been defined or implemented yet. Do not mark a row
implemented until tests or validation evidence exist.

## Status Values

| Status | Meaning |
| --- | --- |
| planned | Accepted as intended behavior, not implemented |
| in_progress | Actively being built |
| implemented | Implemented and proof exists |
| changed | Contract changed after earlier implementation |
| retired | No longer part of the product contract |

## Matrix

| Story | Contract | Unit | Integration | E2E | Platform | Status | Evidence |
| --- | --- | --- | --- | --- | --- | --- | --- |
| [US-001](docs/stories/epics/E01-notifications/US-001-real-event-notifications-and-preferences.md) | Real event notifications and preference controls | implemented | implemented | implemented | no | implemented | backend notification service/controller and the settings/notifications pages are wired to the live preference API; the notifications inbox and settings preferences surface real category toggles, save actions, unread counters, and inbox controls; this sweep verified `frontend` typecheck, `frontend` build, `backend` build, backend unit tests, and Playwright/browser coverage for the notifications and settings notification surfaces |
| [US-003](docs/stories/epics/E03-social-hub/US-003-social-hub-phase-1-core-interactivity.md) | Social Hub Phase 1 core interactivity and analytics contract | implemented | implemented | implemented | no | implemented | backend social-hub controller/service and the dashboard/channels/inbox/calendar pages are wired to the live analytics and inbox contract; the Social Hub overview, dashboard, channels page, publish composer, inbox, and calendar now surface connected accounts, publishing queue, inbox load, page review onboarding, local draft persistence, follow-up handling, planner snapshot data, next scheduled post context, publish checklist, and inbox labels/assignment state; publish now distinguishes backend drafts from immediate queueing and inbox triage persists assignment/labels/follow-up in account metadata; this sweep verified `frontend` typecheck, `frontend` build, `backend` build, `backend` unit tests, and Playwright coverage for `tests/e2e/social-hub.spec.ts` |
| [US-002](docs/stories/epics/E02-admin-managed-content/US-002-admin-managed-content-migration.md) | Admin-managed content migration | implemented | implemented | implemented | no | implemented | backend site-config CRUD plus admin audit logging are live, and the admin content editor plus public site-config readers are wired in the frontend; the admin console exposes the content-config editor with key/locale selectors, JSON editing, and save/reset controls while the landing and stock pages read site-config overrides; this sweep verified `frontend` typecheck, `frontend` build, `backend` build, backend unit tests, and browser coverage of the admin content-config surface |
| [US-000](docs/stories/epics/E00-platform-hardening/US-000-production-hardening-baseline/overview.md) | Production hardening baseline for shared backend services | implemented | implemented | pending | yes | implemented | request IDs and trust-proxy handling exist in backend bootstrap, the frontend API layer propagates x-request-id, direct generation requests keep the request id in metadata, payment return URLs are now sanitized against external redirects, workflow execution snapshots now persist, BullMQ queues now share retry/backoff defaults plus a dead-letter archive for final failures, `/api/health/queues` now reports live queue depth, `/api/v1/admin/queues/dead-letter` lists dead-letter jobs, `/api/v1/admin/queues/dead-letter/:jobId/requeue` recovers them, and structured request logging is covered by unit proof |
| [US-004](docs/stories/epics/E04-creator-tools-hardening/US-004-creator-tools-production-hardening.md) | Creator tools production hardening for assistant, icon, mockup, video editor, and image generator | pending | pending | pending | no | in_progress | frontend build/typecheck passed after hardening AI Assistant, Icon Generator, Mockup Generator, Video Editor, Image Generator, Image Editor, Design Editor, Clip Editor, Workflow Editor, Music Generator, Voice Generator, SFX Generator, Variations, Bg Remover, Image Extender, Image Upscaler, Video Generator, Video Upscaler, Camera Change, Skin Enhancer, Lip Sync, Sketch to Image, workflow editor snapshot persistence, mockup generator backend persistence, variations backend persistence, icon generator backend persistence, ai-assistant backend persistence, and image-editor backend persistence; browser verification succeeded on the creator image/icon/video/mockup/editor/assistant/image-editor/design-editor/clip-editor/workflow-editor/music-generator/voice-generator/sfx-generator/variations/bg-remover/image-extender/image-upscaler/video-upscaler/camera-change, skin-enhancer, lip-sync, sketch-to-image, workflow-editor alias, and spaces shortcut routes; live POST `/api/v1/generations/image` returns `processing` instead of 500; backend billing client now falls back from `billing-service` to `localhost`; workflow execution and create-state loading bugs were fixed, image editor now has real undo/redo/rotate/flip/zoom plus backend project save/export, design editor now persists project content to backend and has working text style toggles, canvas zoom, undo/redo, drag, resize handles, alignment guides, save, and export, clip editor now starts from an empty timeline with draft/project restore, wired playback/seek/duplicate, per-clip trim, clip move, and backend project save/export, video editor now has backend project load/save plus working seek/playback progress, undo/redo, and project export, AI Assistant now wires attachments, template jump actions, and backend project persistence, icon generator now exposes backend project persistence, save/export, and per-result download/copy behavior, voice generator now prefers rendered audio preview/download when available while keeping speech synthesis as a fallback, image generator and video generator now also persist project content to backend storage, audio generators now have reset plus error-safe submit flow, music generator now also renders audio previews from resultUrl and downloads rendered audio blobs when available while sfx generator now mirrors that behavior for generated sound effects, variations and mockup/image/video upscalers now expose save/export/download behavior, bg remover and image extender now persist project content to backend projects while still exposing save/export/reset, image upscaler, video upscaler, camera change, skin enhancer, lip sync, sketch to image, mockup generator, variations, and workflow editor now persist project content or workflow snapshots to backend storage instead of relying only on local draft storage |
| [US-005](docs/stories/epics/E04-creator-tools-hardening/US-005-workflow-editor-node-layout-and-connection-affordances.md) | Workflow editor node layout, connection affordances, and canvas settings | pending | pending | pending | no | implemented | browser verification showed new nodes no longer stacking on one fixed point, the settings modal now changes real workflow behavior, and workflow handle wiring was restored for connect flows; utility and legacy nodes now render real connection handles, connection snapshots now mirror prompt/image/video aliases into panel state, with DOM verification for Text and Image Generator handles; rebuilt `ai-generator-web` and verified Image Generator prompt textarea remains editable in hand/pan mode on `http://localhost/creator/workflow-editor`; shared workflow node shells and toolbars were restyled so edge ports stay visible on the card border, connector affordances are tighter and easier to discover, workflow ports now render visible type badges instead of plain dots, and the workflow canvas now exposes real Save/Reset/Export actions plus execution state fixes so sync executions and workflow creation no longer leave the UI stuck loading |
| [US-006](docs/stories/epics/E05-community-marketplace/US-006-community-marketplace-production-hardening.md) | Community Marketplace production hardening for browse, publish, preview, and purchase flows | implemented | implemented | implemented | no | implemented | backend marketplace controller/service are wired for browse, publish, preview, and purchase; `backend/src/community-marketplace/community-marketplace.service.spec.ts` now proves purchase is transactional with rollback on insufficient credits, and the full backend suite passes; browser verification covered browse cards, type filters, and publish form controls on the Community route after sign-in, with search/filter state changing the live listing surface |

- `orgs/new`, `orgs/[slug]/projects`, `orgs/[slug]/invites`, `projects/[projectId]`, `social/calendar`, `social/channels`, `social/publish`, and `visual-flow/projects/[id]` now preserve create-form drafts in localStorage, restore them on reopen, and clear the draft after a successful create; the modal/page flows also stop persisting after successful submit so an empty reset cannot rewrite the cleared draft, including the workflow create dialog plus the visual-flow add-scene and add-video dialogs.

## Evidence Rules

- Unit proof covers pure domain and application rules.
- Integration proof covers backend enforcement, data integrity, provider
  behavior, jobs, or service contracts.
- E2E proof covers user-visible browser flows.
- Platform proof covers only shell, deployment, mobile, desktop, or runtime
  behavior that cannot be proven in lower layers.
- A story can be implemented without every proof column if the story packet
  explains why.
