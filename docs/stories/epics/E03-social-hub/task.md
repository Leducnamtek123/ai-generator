# Social Hub Phase 1 Task

## Goal

Replace the remaining hardcoded Social Hub dashboard analytics with backend data
and record the Phase 1 contract in the harness.

## Scope

- Backend analytics now accept a `days` window.
- Dashboard audience distribution renders from `platformBreakdown`.
- Story and test matrix entries exist for the Social Hub contract.

## Notes

- Do not widen into Calendar Phase 2 or new calendar dependencies in this task.
- Keep inbox reply/back-end automation for a follow-up slice if it needs a new
  endpoint contract.
